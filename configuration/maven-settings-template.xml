<?xml version="1.0" encoding="UTF-8"?>
<!--
  Maven settings.xml template for cross-platform-functional-test-automation-sdk
  Copy this to: ~/.m2/settings.xml (or merge into your existing settings.xml)

  For publishing, use a GitHub token with packages write scope.
  For consumption, use a GitHub token with packages read scope.
-->
<settings xmlns="http://maven.apache.org/SETTINGS/1.1.0"
          xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://maven.apache.org/SETTINGS/1.1.0 http://maven.apache.org/xsd/settings-1.1.0.xsd">

  <!-- NYC proxy -- required on-premise; remove if working remotely without proxy -->
  <proxies>
    <proxy>
      <id>http-proxy-settings</id>
      <active>true</active>
      <protocol>http</protocol>
      <host>bcpxy.nycnet</host>
      <port>8080</port>
      <nonProxyHosts>10.*|192.168.*|172.16.*|*.nycnet|localhost|github.com|*.github.com|maven.pkg.github.com</nonProxyHosts>
    </proxy>
  </proxies>

  <servers>
    <!--
      Server ID MUST match the <id> in pom.xml <distributionManagement><repository>
      Username: your GitHub username, supplied through GITHUB_USERNAME
      Password: your GitHub token (with packages write scope for publishing)
    -->
    <server>
      <id>github</id>
      <username>${env.GITHUB_USERNAME}</username>
      <password>${env.GITHUB_TOKEN}</password>
      <configuration>
        <httpConfiguration>
          <all>
            <usePreemptive>true</usePreemptive>
          </all>
        </httpConfiguration>
      </configuration>
    </server>
  </servers>

</settings>
